import React from 'react';
import ReactDOM from 'react-dom';
import CongregationTracker from './components/service-report-assistant';

ReactDOM.render(
  <React.StrictMode>
    <CongregationTracker />
  </React.StrictMode>,
  document.getElementById('root')
);